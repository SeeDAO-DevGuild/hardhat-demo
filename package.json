{
	"name": "hardhat-demo",
	"version": "0.0.0",
	"description": "This project demonstrates a basic Hardhat use case",
	"main": "index.ts",
	"scripts": {
		"compile": "rimraf artifacts cache && npx hardhat compile --force",
		"test": "npx hardhat test",
		"deploy": "npx hardhat run scripts/deploy.ts --network sepolia"
	},
	"author": "victor",
	"license": "ISC",
	"devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^4.0.0",
		"hardhat": "^2.19.3",
		"rimraf": "^5.0.5"
	},
	"dependencies": {
		"dotenv": "^16.3.1"
	}
}
